import React from 'react';
import ChatContainer from './ChatContainer';
import ChatBoxes from './ChatBoxes';

const Chat = () => {
    return (
        <ChatContainer>
            <ChatBoxes />
        </ChatContainer>
    );
}

export default Chat;